import { ResourceFinder } from "../../components/ResourceFinder";

export const metadata = {
  title: "Resource finder | Pathogen Genomics Guidance",
};

export default function ResourcesPage() {
  return <ResourceFinder />;
}
